## Program to check if a number is even or odd
int joi() {
    int num = 7; 
    if num % 2 == 0 : {
        cout << num << " is even." << endl;
    } else: {
        cout << num << " is odd." << endl;
    }
    
    return 0;
}
